# Migration `20191117210945-initial-migration`

This migration has been generated by Peter Pistorius at 11/17/2019, 9:09:45 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT 
);

CREATE TABLE "lift"."AccessToken" (
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "sub" TEXT NOT NULL DEFAULT ''  ,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL
);

CREATE TABLE "lift"."Invoice" (
  "body" TEXT NOT NULL DEFAULT ''  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "date" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "invoiceNumber" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "lift"."AccessToken.sub" ON "AccessToken"("sub")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191117210945-initial-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,31 @@
+datasource hammerDatasource {
+  provider = "sqlite"
+  url = "file:dev.db"
+}
+
+generator photonjs {
+  provider = "photonjs"
+  output = "../../packages/photon"
+}
+
+model User {
+  id                Int               @id
+  accessTokens      AccessToken[]
+  invoices          Invoice[]
+}
+
+model AccessToken {
+  id                Int               @id
+  user              User?
+  sub               String            @unique
+}
+
+model Invoice {
+  id                String            @default(cuid()) @id
+  user              User
+  invoiceNumber     String
+  date              String
+  body              String
+  createdAt         DateTime          @default(now())
+  updatedAt         DateTime          @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191117210945-initial-migration)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191117210945-initial-migration'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
