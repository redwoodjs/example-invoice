# Migration `20191012145049-add-invoice-info-fields`

This migration has been generated by Peter Pistorius at 10/12/2019, 2:50:49 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Invoice" (
  "body" TEXT NOT NULL DEFAULT ''  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "date" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "invoiceNumber" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Invoice" ("body","createdAt","id","updatedAt") SELECT "body","createdAt","id","updatedAt" from "Invoice"

DROP TABLE "lift"."Invoice";

ALTER TABLE "lift"."new_Invoice" RENAME TO "Invoice";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190922181550-convert-int-id-to-string..20191012145049-add-invoice-info-fields
--- datamodel.dml
+++ datamodel.dml
@@ -1,12 +1,12 @@
-datasource sqlite {
-  url      = "file:dev.db"
-  provider = "sqlite"
+datasource hammerDatasource {
+  provider = env("PRISMA_PROVIDER")
+  url = env("PRISMA_URL")
 }
 generator photonjs {
   provider = "photonjs"
-  output = "../generated/photon"
+  output = "../../packages/photon"
 }
 model User {
   id                Int               @id
@@ -15,15 +15,17 @@
 }
 model AccessToken {
   id                Int               @id
-  user              User?             
+  user              User?
   sub               String            @unique
 }
 model Invoice {
   id                String            @default(cuid()) @id
-  User              User              
+  user              User
+  invoiceNumber     String
+  date              DateTime          @default(now())
   body              String
   createdAt         DateTime          @default(now())
   updatedAt         DateTime          @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191012145049-add-invoice-info-fields)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191012145049-add-invoice-info-fields'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
