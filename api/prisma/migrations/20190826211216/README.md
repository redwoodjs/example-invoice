# Migration `20190826211216`

This migration has been generated by Peter Pistorius at 8/26/2019, 9:12:16 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."AccessToken"("id" INTEGER NOT NULL DEFAULT 0 ,"sub" TEXT NOT NULL DEFAULT '' ,"user" INTEGER   REFERENCES User(id),PRIMARY KEY ("id"));

PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_User"("id" INTEGER NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"));

INSERT INTO "new_User" ("id") SELECT "id" from "User"

DROP TABLE "lift"."User";

ALTER TABLE "lift"."new_User" RENAME TO "User";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;

CREATE UNIQUE INDEX "lift"."AccessToken.sub._UNIQUE" ON "AccessToken"("sub")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190720132201-initial-database..20190826211216
--- datamodel.dml
+++ datamodel.dml
@@ -14,9 +14,9 @@
 }
 model AccessToken {
   id                Int               @id
-  user              User              @nullable
+  user              User?             
   sub               String            @unique
 }
 model Invoice {
```

## Photon Usage

You can use a specific Photon built for this migration (20190826211216)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190826211216'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
