# Migration `20190922181550-convert-int-id-to-string`

This migration has been generated by Peter Pistorius at 9/22/2019, 6:15:50 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Invoice" (
  "User" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL,
  "body" TEXT NOT NULL DEFAULT ''  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Invoice" ("User","body","createdAt","id","updatedAt") SELECT "User","body","createdAt","id","updatedAt" from "Invoice"

DROP TABLE "lift"."Invoice";

ALTER TABLE "lift"."new_Invoice" RENAME TO "Invoice";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190826211216..20190922181550-convert-int-id-to-string
--- datamodel.dml
+++ datamodel.dml
@@ -4,8 +4,9 @@
 }
 generator photonjs {
   provider = "photonjs"
+  output = "../generated/photon"
 }
 model User {
   id                Int               @id
@@ -19,10 +20,10 @@
   sub               String            @unique
 }
 model Invoice {
-  id                Int               @id
-  User              User
+  id                String            @default(cuid()) @id
+  User              User              
   body              String
   createdAt         DateTime          @default(now())
   updatedAt         DateTime          @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190922181550-convert-int-id-to-string)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190922181550-convert-int-id-to-string'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
