# Migration `20191012172407`

This migration has been generated by Peter Pistorius at 10/12/2019, 5:24:07 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Invoice" (
  "body" TEXT NOT NULL DEFAULT ''  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "date" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "invoiceNumber" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" INTEGER    REFERENCES "User"(id) ON DELETE SET NULL,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Invoice" ("body","createdAt","date","id","invoiceNumber","updatedAt","user") SELECT "body","createdAt","date","id","invoiceNumber","updatedAt","user" from "Invoice"

DROP TABLE "lift"."Invoice";

ALTER TABLE "lift"."new_Invoice" RENAME TO "Invoice";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191012145049-add-invoice-info-fields..20191012172407
--- datamodel.dml
+++ datamodel.dml
@@ -23,9 +23,9 @@
 model Invoice {
   id                String            @default(cuid()) @id
   user              User
   invoiceNumber     String
-  date              DateTime          @default(now())
+  date              String
   body              String
   createdAt         DateTime          @default(now())
   updatedAt         DateTime          @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191012172407)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191012172407'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
