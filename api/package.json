{
  "private": true,
  "name": "api",
  "version": "1.0.0",
  "dependencies": {
    "@hammerframework/hammer-api": "0.0.0",
    "dotenv": "^8.0.0",
    "graphql": "^14.3.1",
    "jsonwebtoken": "^8.5.1",
    "jwks-rsa": "^1.5.1",
    "nexus": "^0.11.7",
    "prisma2": "^2.0.0-preview-2"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "^3.2.0",
    "@netlify/zip-it-and-ship-it": "^0.3.1"
  },
  "scripts": {
    "dev": "yarn db:generate && yarn workspace @hammerframework/hammer-dev-server dev",
    "db:generate": "yarn prisma2 generate",
    "db:migrate": "yarn prisma2 lift up",
    "build": "yarn clean && NODE_ENV=production babel src --out-dir dist && node ./scripts/package-functions.js",
    "clean": "rm -rf dist"
  }
}
