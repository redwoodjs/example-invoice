{
  "private": true,
  "name": "rwjs-auth",
  "version": "0.0.0",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "yarn build:js",
    "build:js": "yarn babel src -d dist --delete-dir-on-start --extensions \".js,.ts,.tsx\" --source-maps inline",
    "build:watch": "nodemon --watch src --ext 'js,ts,tsx' --ignore dist --exec 'yarn build'"
  },
  "devDependencies": {
    "@auth0/auth0-spa-js": "^1.7.0",
    "gotrue-js": "^0.9.25",
    "@types/react": "^16.9.34",
    "nodemon": "^2.0.3"
  }
}
